meta {
  name: Delete Dam
  type: http
  seq: 13
}

delete {
  url: {{base_url}}/dams/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

assert {
  res.status: eq 204
}

tests {
  test("Status code is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
