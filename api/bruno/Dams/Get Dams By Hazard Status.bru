meta {
  name: Get Dams By Hazard Status
  type: http
  seq: 9
}

get {
  url: {{base_url}}/dams/hazard-status/safe
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

assert {
  res.status: eq 200
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Response contains dams array", function() {
    const data = res.getBody();
    expect(data).to.be.an('array');
  });
}
