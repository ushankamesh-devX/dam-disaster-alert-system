meta {
  name: Get Dams By Status
  type: http
  seq: 8
}

get {
  url: {{base_url}}/dams/status/operational
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

assert {
  res.status: eq 200
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Response contains dams array", function() {
    const data = res.getBody();
    expect(data).to.be.an('array');
  });
}
