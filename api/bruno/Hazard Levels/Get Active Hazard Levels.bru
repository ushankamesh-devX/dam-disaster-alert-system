meta {
  name: Get Active Hazard Levels
  type: http
  seq: 2
}

get {
  url: {{base_url}}/hazard-levels/active
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

assert {
  res.status: eq 200
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Response contains active hazard levels", function() {
    const data = res.getBody();
    expect(data).to.be.an('array');
  });
}
