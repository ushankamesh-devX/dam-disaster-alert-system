meta {
  name: Delete Hazard Zone
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/dams/hazard-zones/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

assert {
  res.status: eq 204
}

tests {
  test("Status code is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
