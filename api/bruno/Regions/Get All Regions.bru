meta {
  name: Get All Regions
  type: http
  seq: 1
}

get {
  url: {{base_url}}/regions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

assert {
  res.status: eq 200
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Response contains regions array", function() {
    const data = res.getBody();
    expect(data).to.be.an('array');
  });
}
