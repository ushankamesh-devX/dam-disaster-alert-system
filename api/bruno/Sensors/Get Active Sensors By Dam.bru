meta {
  name: Get Active Sensors By Dam
  type: http
  seq: 4
}

get {
  url: {{base_url}}/sensors/dam/1/active
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

assert {
  res.status: eq 200
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Response contains active sensors", function() {
    const data = res.getBody();
    expect(data).to.be.an('array');
  });
}
