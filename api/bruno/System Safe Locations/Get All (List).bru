meta {
  name: Get All (List)
  type: http
  seq: 1
}

get {
  url: {{base_url}}/system-safe-locations/list
  body: none
}

assert {
  res.status: eq 200
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
